# DeepSeek 桌面悬浮窗

一个小型 Windows 悬浮窗，显示 DeepSeek API 余额、今日 Token 用量和今日消费，支持拖动、收起、手动刷新和自动刷新。

## 启动

双击 `start-widget.bat` 即可启动。启动后窗口默认出现在屏幕右上角。

## 首次使用需要做的两件事

1. 获取 API Key
   - 打开 DeepSeek 平台：https://platform.deepseek.com
   - 登录后进入 API Keys 页面，创建或复制一个 API Key。
   - 在悬浮窗右上角点设置按钮，把 API Key 粘贴到“API Key”输入框。

2. 授权获取“用量 Token”
   - 在同一个设置窗口点“一键授权获取用量 Token”。
   - 会打开一个独立的 Edge 窗口，请在其中登录 DeepSeek 平台（可能包含手机验证码）。
   - 登录并停留在用量页面后，授权会自动完成并填入输入框，点“保存”即可。
   - 如果一键授权不可用，可用手动方式：打开平台用量页并登录，按 F12 → Network → 刷新页面 → 找到 `usage/amount` 请求 → 复制 `Authorization` 中 Bearer 后面的内容，粘贴到“用量 Token”。

保存后悬浮窗会立即刷新，显示余额、今日 Token 和今日消费。

## 说明

- 余额来自 DeepSeek 官方接口 `/user/balance`。
- 今日 Token 和消费来自 DeepSeek 平台用量接口。DeepSeek 没有开放官方的用量查询 API，因此需要一次登录授权；Token 失效后重新授权一次即可。
- 默认每 1 分钟自动刷新，窗口重新激活时也会刷新，可在设置中改为 1-120 分钟。
- API Key 和用量 Token 只保存在本机 `config.json`，保存后会用 Windows 当前用户加密，不会上传到任何地方。
- 想开机自启，把 `start-widget.bat` 的快捷方式放入“启动”文件夹（按 Win+R 输入 `shell:startup` 回车即可打开）。
